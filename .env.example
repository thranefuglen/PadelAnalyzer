# Environment variables template
# Copy this file to .env.local for local development

# No environment variables required for this MVP
# All configuration is handled in the code

# Optional: Set custom storage path (defaults to ./storage)
# STORAGE_PATH=/path/to/custom/storage

# Optional: Set custom Python command (defaults to 'python')
# PYTHON_COMMAND=python3

# Optional: Set custom ffmpeg path (defaults to 'ffmpeg')
# FFMPEG_PATH=/usr/local/bin/ffmpeg